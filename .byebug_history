c
  user = User.find(2)
        @book = user.books.new(book_params)
@book = user.books.new(book_params)
user = User.find(2)
c
 user1 = User.find(user_id: 2)
        @book = user1.books.new(book_params)
 user1 = User.find(2)
        @book = user1.books.new(book_params)
 user1 = curent_user.id
        @book = user1.books.new(book_params)
 user1 = User.find(2)
        @book = user1.books.new(book_params)
 user = User.find(2)
        @book = user.books.new(book_params)
 user = User.find(params[id: 2])
        @book = user.books.new(book_params)
 user = User.find(params[2])
        @book = user.books.new(book_params)
 user = User.find(params[:id])
        @book = user.books.new(book_params)
 @book = Book.new(book_params)
c
current_user.books
@book = current_user.books.build(book_params)
current_user.books
current_user.build.books
current_user.build.book(book_params)
current_user.build_book(book_params)
current_user.build(Book.new)
current_user.build_book
current_user
User.first.books
User.first.book
User.first
Book
@book.user = current_user
@book = Book.new(book_params)
