<script>
    let stripe = Stripe("<%= Rails.application.credentials[:stripe][:public]%>")
    stripe.redirectToCheckout({
        sessionId: '<%= @session.id %>'
        
    })
</script>
<p>helllooifdsfjdlkjf</p>